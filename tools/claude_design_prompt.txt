Generate a 11-slide presentation for a 15-minute talk. Style: clean, professional, blue/white corporate theme. Minimal text per slide — mostly visuals.

## Project
AI pipeline that analyzes insurance claim videos (car or house damage) and outputs a structured recap for human supervisors.

## Key Numbers
- 2,971 labeled images, 100 damage classes
- YOLOv11n (nano) for detection
- 300K-parameter causal transformer for recap text (no external LLM)
- 3ms recap inference on CPU
- ~2 min processing per 30s video
- 5 insurance categories: collision, water damage, fire damage, storm impact, wear & tear

## Slide Structure

1. **Title**: "AI-Assisted Insurance Damage Assessment — Car & Property Damage from Video"
2. **Problem**: Claims handlers spend 15-30 min per video. 20% human error rate. Target: <2 min automated.
3. **Pipeline diagram** (flowchart): Video → Keyframes → YOLO Detection → Temporal Aggregation → Problem Classifier → Recap Transformer → Structured Recap. Two training branches (YOLO + Recap) feeding into the pipeline.
4. **Dataset**: 2,971 images merged from 3 sources. 100 classes. Imbalance challenge (5-80 samples per class). Show a bar chart.
5. **Training**: YOLOv11n transfer-learned. Key optimizations: freeze backbone 10 epochs, warmup + cosine LR, RAM caching, mixed precision FP16.
6. **Inference**: 5 steps — scene detection, YOLO per frame, IOU tracking, rule classifier, recap generation. ~2 min end-to-end.
7. **Recap model**: GPT-style causal transformer. 300K params, 4 layers, self-attention only. Trained on 20K synthetic samples. 3ms inference. No external API.
8. **Results table** (leave cells blank for [X]):
   | Metric | Target | Achieved |
   |--------|--------|----------|
   | Detection mAP@0.5 | 0.60 | [X] |
   | Keyframe coverage | 0.90 | [X] |
   | Processing time | 2 min | [X] |
   | Recap accuracy | 0.80 | [X] |
9. **Failure analysis**: 3 cases — missed hairline crack (resolution), pre-existing scratch flagged as new (no temporal context), overcount in frames (IOU linking).
10. **Limitations + Future**: No temporal comparison, imbalanced data, 640px context window. Future: pre/post video comparison, synthetic augmentation, human-in-the-loop.
11. **Thank you / Q&A**: GitHub link github.com/ANTI-AXIOM/AI-3J

## Visual Assets Needed
- Pipeline flowchart (slide 3)
- Sample grid of damage images (slide 4)
- Training loss/throughput curves (slide 5)
- Detection examples with bounding boxes (slide 6)
- Terminal screenshot of recap output (slide 7)
- Bar chart of class distribution (slide 4)
